type Author {
    id: ID!
    firstName: String!
    lastName: String!
    createdAt: Int
    updatedAt: Int
    deletedAt: Int
}

input AuthorCreateInput {
    firstName: String!
    lastName: String!
}

input AuthorUpdateInput {
    id: ID!
    firstName: String
    lastName: String
}

input AuthorDeleteInput {
    id: ID!
}

type AuthorsPayload {
    authors: [Author!]
    total: Int!
}